{
  "name": "agriculture",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "venv\\Scripts\\python -m uvicorn app.main:app --reload --port 8000 --app-dir backend",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "venv\\Scripts\\pip install -r backend\\requirements.txt && cd frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
